# 核心依赖（txt/json/在线模型调用零第三方依赖，纯标准库）
# Python >= 3.11 内置模块：re, sqlite3, dataclasses, pathlib, argparse, json, urllib

# 可选：Excel 格式支持（.xlsx / .xls）
openpyxl>=3.1
xlrd>=2.0

# 可选：GUI 可视化（charts.py；不装则可视化标签页显示提示，其余功能不受影响）
matplotlib>=3.8

# 可选：生成 .xls 样例文件（仅开发用）
# xlwt>=1.3

# 可选：测试
# pytest>=8.0
